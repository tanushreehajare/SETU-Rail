command:
  - streamlit
  - run
  - app.py

env:
  - name: DASHBOARD_URL
    value: ""
  # Paste your published AI/BI dashboard embed URL above.
  # Format: https://<workspace>.cloud.databricks.com/embed/dashboardsv3/<id>
  # Get it from: Dashboard → Share → Embed
